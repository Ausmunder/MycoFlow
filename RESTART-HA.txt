=================================================================
RESTART BACKEND ON HOME ASSISTANT - v4.7.0
=================================================================

ROOT CAUSE FIXED: Database hostname changed from 'postgres' to 'sopp-tracker-db'

SIMPLE FIX: Just run the restart script on Home Assistant terminal

=================================================================
RUN THIS COMMAND ON HOME ASSISTANT TERMINAL:
=================================================================

bash /config/sopp-tracker/restart.sh

=================================================================
WHAT THE SCRIPT DOES:
=================================================================

1. Stops and removes old container
2. Starts new container with:
   - Correct DATABASE_URL using sopp-tracker-db hostname
   - Volume mounts for live code reload
   - Port 8000 exposed
3. Runs migration script
4. Tests API endpoint

=================================================================
VERIFY SUCCESS:
=================================================================

You should see at the end:
- "version": "4.7.0"
- Container running without restart loops
- Migration completed successfully

Test workflow endpoint:
curl http://localhost:8000/api/batches/1/workflow

=================================================================
FILES DEPLOYED:
=================================================================

Backend v4.7.0 files are ready at k:\sopp-tracker\backend\:
✓ app/main.py (v4.7.0)
✓ app/models.py (workflow columns)
✓ app/routers/workflow.py (NEW)
✓ app/utils/workflow_predictor.py (NEW)
✓ scripts/migrate_workflow_v4_7.sql (NEW)
✓ scripts/run_migration.py (NEW)
✓ docker-compose.yml (FIXED DATABASE_URL)
✓ restart.sh (READY TO RUN)

=================================================================
